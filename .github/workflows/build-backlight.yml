name: Build Backlight Tools

on:
  workflow_call:
  push:
    paths:
      - 'rpi/backlight/**'
  pull_request:
    paths:
      - 'rpi/backlight/**'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-arm-linux-gnueabihf gcc-aarch64-linux-gnu

      - name: Build
        run: |
          cd rpi/backlight
          make

      - name: Upload binaries
        uses: actions/upload-artifact@v4
        with:
          name: backlight-bin
          path: rpi/backlight/bin/
